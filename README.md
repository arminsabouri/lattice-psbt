# Lattice-PSBT

This library creates a monotone data structure that can take minimally defined transaction components and merge or join them if they are non-conflicting. The design ensures eventual consistency by modeling a PSBT as a join semilattice set.
